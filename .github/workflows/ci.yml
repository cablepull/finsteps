name: CI (disabled – manual only)

# Temporarily disabled from automatic runs.
# You can still run this workflow manually from the Actions tab if needed.
on:
  workflow_dispatch: {}

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: ./scripts/ci-build.sh
